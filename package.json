{
  "license": "BSD-3-Clause",
  "scripts": {
    "postinstall": "elm-tooling install",
    "test": "elm-format --yes src/ && elm make src/ReviewConfig.elm --output=/dev/null"
  },
  "dependencies": {
    "elm-tooling": "^1.1.0"
  }
}
