{
  "license": "BSD-3-Clause",
  "scripts": {
    "test": "elm-format --validate src/ && elm make src/ReviewConfig.elm --output=/dev/null"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-json": "^0.2.7"
  }
}
